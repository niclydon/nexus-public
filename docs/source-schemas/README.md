# Source Schemas

This directory holds public-facing payload schema docs for push-style sources.

Use one file per source:

- `docs/source-schemas/<source>.md`

Keep these docs structural and implementation-focused. Do not include private
hostnames, tokens, device identifiers, or environment-specific paths.
