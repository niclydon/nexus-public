# Test Fixtures

This directory holds sample source payloads and expected normalized outputs used
for onboarding and smoke verification.

Use one file per source:

- `tests/fixtures/<source>.json`

Fixtures in the public repo should stay synthetic or sanitized.
